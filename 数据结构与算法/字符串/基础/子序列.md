# 定义
---
>[!note] 子序列
>字符串的子序列指从字符串中取出零个或多个字符并在不改变字符相对顺序的情况下所组成的新字符串。

>[!tip] [[子序列]]与[[子串]]的区别
> 子序列只要求子字符保持原有相对顺序而不要求连续，子串既要子字符保持原有相对顺序也要子字符连续。
# 示例
---
对于字符串`abc`，子序列有：` `、  `a`、 `b`、 `c`、`ab`、`ac`、`bc`、`abc`。