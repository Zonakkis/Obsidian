---
时间复杂度: O(N^3)
空间复杂度: O(N^2)
---
# 定义
用于求任意两个顶点间的最短路。
不适用的情况：存在负环。
使用三层for(i,j,k)求出各点间的最短距离：
k:中间节点。
i:起始顶点。
j:目标顶点。
# 模版
```c++
int n, d[n][n];
for (k = 1; k <= n; k++) 
	for (i = 1; i <= n; i++)
		for (j = 1; j <= n; j++) 
			d[i][j] = min(d[i][j], d[i][k] + d[k][j]);
```