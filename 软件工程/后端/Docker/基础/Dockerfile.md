# 定义
---
>[!note] Dockerfile
>Dockerfile是一个包含构建指令的文本文件，Docker 会根据其中的指令构建出一个镜像。
# 指令
---

| 指令     | 示例                      | 描述     |
| ------ | ----------------------- | ------ |
| `FROM` | `FROM python:3.11-slim` | 指定基础镜像 |
# 例子
---
