# insert into
向表中插入新纪录。
```sql
insert into <table_name> values (<values>);
insert into <table_name> (<columns>) values (<values>);
```
`<table_name>`：要插入新纪录的表名称。
`<columns>`：要插入的字段名称。
`<values>`：要插入的字段值。
例：
```sql
insert into user (id,name,age) values (5,'王五',22);
```
