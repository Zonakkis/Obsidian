# update
更新表中的记录。
```sql
update table_name set <columns = values>,... where <conditions>;
```
`<table_name>`：要更新纪录的表名称。
`<columns>`：要更新的字段名称。
`<values>`：要更新的字段值。
`<conditions>`：筛选条件。（如果没有筛选条件表中所有记录都将被更新。）
例：
```sql
update user set name = '李四', age = 19 where id = 1;
```
