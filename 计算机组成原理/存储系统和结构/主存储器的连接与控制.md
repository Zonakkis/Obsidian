# 主存储器与CPU的连接
主存储器与CPU之间有三种总线：
- **地址总线**：CPU 通过地址总线发出地址码（，经过存储器内部的**地址译码器**），选中特定的存储单元。地址总线的位数决定了 CPU 的最大寻址范围（如 32 位地址线对应 $2^{32} = 4GB$ 空间）。
- **数据总线**：它是 CPU 与存储器之间交换信息的双向通道。数据线的位数通常与存储器的**字长**一致（如 32 位或 64 位）。
- **控制总线**：负责传输操作信号，确保读写动作同步。
