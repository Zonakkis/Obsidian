# 命名空间
`namespace std;`
# 作用
返回点(x, y)的方位角。范围为(-π, π]。
# 声明
```c++
float       atan2 (float y, float x);  

double      atan2 (double y, double x);  

long double atan2 (long double y, long double x);
```
`y`：点的y坐标。
`x`：点的x坐标。