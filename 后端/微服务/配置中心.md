在传统的单体应用中，采用静态配置文件（如application.yml）打包在代码中，修改了配置后需要重启应用。而在微服务中，可能多个服务存在重复的配置，还需要管理大量微服务实例，十分繁琐，因此需要配置中心来管理配置文件。
# 核心能力
---
- **集中管理**：所有服务配置统一存放。
- **动态刷新（热更新）**：无需重启，修改即生效。
    
- **多环境隔离**：一键切换环境。
    
- **版本控制与回滚**：支持历史版本比对与一键回退。
    
- **灰度发布**：只让部分节点先应用新配置。