服务对应的配置文件Data ID规则为：
```yaml
${spring.application.name}.${spring.cloud.nacos.config.file-extension}
${spring.application.name}-${spring.prodiles.active}.${spring.cloud.nacos.config.file-extension}
```
比如：`user-service.yaml`、`user-service-dev.yaml`