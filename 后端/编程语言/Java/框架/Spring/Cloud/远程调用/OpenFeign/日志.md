OpenFeign有四种日志级别：
- `NONE`	默认值，不记录任何日志。	生产环境默认配置
- `BASIC`	仅记录请求方法、URL、响应状态码及执行耗时。	生产环境定位性能问题或接口响应
- `HEADER`S	包含 BASIC 的内容，额外增加请求头和响应头信息。	需要排查 Token、Cookie 或 Header 传输时
- `FULL`	记录完整的请求与响应 Header、Body（正文）及元数据。	本地开发调试、排查接口入参和返参问题