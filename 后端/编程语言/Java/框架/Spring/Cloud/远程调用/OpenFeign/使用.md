# 编写客户端
---
```java
@FeignClient("user-service") // 注册中心里的服务名
public interface UserClient {

	@GetMapping("/users")
	List<User> getUsersByIds(@RequestParam("ids") Collection<Long> ids);
}
```
## 使用
---
```java
public class OtherService {
	
	@Autowired
	private UserClient userClient;
	
	public void foo(List<Long> ids){
		List<User> users = userClinet.getUsersByIds(ids);
		// 业务...
	}
}
```